//! Interface contract for [`crate::core::DefaultConfigBuilder`].

/// Default semantic version applied to a builder that has not been given an explicit version.
pub(crate) const DEFAULT_VERSION: &str = "0.1.0";
