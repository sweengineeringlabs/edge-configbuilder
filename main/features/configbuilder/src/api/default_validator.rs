//! Interface contract for [`crate::core::DefaultValidator`].

/// Error message emitted when a config path exists but is not a directory.
pub(crate) const NOT_A_DIR_MSG: &str = "config path exists but is not a directory";
