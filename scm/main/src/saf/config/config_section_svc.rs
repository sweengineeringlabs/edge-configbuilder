//! SAF marker module for the `ConfigSection` API trait.
// @allow: shared_prefix_grouping — SAF marker filenames must match trait names.
#![allow(dead_code)]

pub const CONFIG_SECTION_SVC: &str = "ConfigSection";
