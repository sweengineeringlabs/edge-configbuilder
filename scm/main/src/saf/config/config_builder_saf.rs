//! SAF marker module for the `ConfigBuilder` API trait.
// @allow: shared_prefix_grouping — SAF marker filenames must match trait names.
#![allow(dead_code)]

/// Hidden marker used to make the SAF facade visible to the crate root.
pub const CONFIG_BUILDER_SVC: &str = "ConfigBuilder";
