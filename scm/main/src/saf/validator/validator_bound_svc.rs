//! SAF marker module for the `ValidatorBound` API trait.
// @allow: shared_prefix_grouping — SAF marker filenames must match trait names.
#![allow(dead_code)]

pub const VALIDATOR_BOUND_SVC: &str = "ValidatorBound";
